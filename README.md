# エニグマ暗号文単独攻撃ツール

エニグマ M3（陸軍3ローター式、リフレクター B）の暗号文を、平文の手がかり
（クリブ）なしで復号するツールです。英語と日本語ローマ字の両方に対応。

## ファイル構成

```
enigma_breaker/
├── enigma.py                    # エニグマM3シミュレータ（自前実装）
├── corpora.py                   # 言語モデル構築用の英文・ローマ字コーパス
├── scoring.py                   # n-gram 言語スコアリング
├── attack.py                    # Gillogly 二段階攻撃（通常・精度・徹底モード）
├── decrypt.py                   # コマンドラインエントリ（対話モード対応）
├── decrypt_known_plugboard.py   # プラグボード既知版解読ツール
├── decrypt_plugboard.py         # プラグボード未知・最高精度版（段階スコア）
├── wordlist.py                  # 短文スコア用の英単語リスト（手選別・フォールバック）
├── wordlist_data.py             # 拡張英単語リスト（8800語, 自動生成）
├── ngrams_en.txt.gz             # 大規模コーパス由来の n-gram 頻度表（gzip 224KB）
├── enigma_decoder/              # Rust + Rayon 高速コア（PyO3）
└── README.md                    # このファイル
```

純Python部分は外部依存ゼロ（標準ライブラリのみ）。Python 3.7+ で動作。
Rust コア（`enigma_decoder`）はオプションで、ビルドすれば数十〜80倍高速化する。

## 精度を左右する言語モデル

解読の成否は「復号文が自然言語らしいか」を測る言語モデルの質でほぼ決まる。
本ツールは大規模コーパス（Pride and Prejudice / War and Peace / Shakespeare、
計 約390万字）から事前計算した n-gram 頻度表 `ngrams_en.txt.gz` を同梱している。

| | 旧（組み込み小コーパス）| 現（大規模 n-gram 表）|
|---|---|---|
| コーパス | 約3千字 | **約390万字** |
| trigram 網羅 | 8.3% | **48.9%（実用ほぼ全域）** |
| quadgram 網羅 | 0.5% | **15.3%** |
| 単語リスト | 982語 | **8,806語** |

**精度への効果**（プラグボード未知・段階スコア・50〜150字×2〜10ペア 10ケース）:

| 言語モデル | 完全解読 | 文字一致率 |
|---|---|---|
| 小コーパス | 7/10 | 93.0% |
| **大規模 n-gram 表** | **10/10** | **100%** |

`ngrams_en.txt.gz` が無い環境では自動的に組み込みコーパスへフォールバックする
（精度は落ちるが動作する）。頻度表は以下で再生成できる（任意のコーパスで差し替え可）。

短文の到達限界（プラグボードあり・暗号文単独）: 大規模モデルで **約34字**まで
解読可能を確認。29〜33字はユニシティ距離を下回り、どの手法でも不安定。

## プラグボード未知・最高精度版 (`decrypt_plugboard.py`)

プラグボードが**分からない**状態から全設定（ローター・位置・リング・プラグボード）を
復元する、最高精度の解読機。文献
[Ostwald & Weierud 2017, "Modern breaking of Enigma ciphertexts"](https://www.tandfonline.com/doi/abs/10.1080/01611194.2016.1238423)
の**三段階スコアエスカレーション**を実装している。

```bash
python decrypt_plugboard.py             # 対話モード
python decrypt_plugboard.py --selftest  # 動作確認（155字・6ペアを復元）
python decrypt_plugboard.py --lang english <暗号文>
```

**解読フェーズ**:

```
Phase A  ローター順＋初期位置 … IC + bigram-IC で全探索（プラグボード不変性を利用）
Phase C  プラグボード復元      … IC → bigram → trigram の三段階山登り
                                ＋マルチスタート＋SA 磨き上げ（Rust/Rayon 並列）
Phase B  リング設定            … 右→中ローターのリングを再探索
Phase D  最終検証              … n-gram＋単語リスト照合で候補確定
```

**なぜ段階スコアが効くのか**: プラグボードが数本しか合っていない段階では復号文は
まだランダムに近く、trigram/quadgram はノイズだらけで正しい方向を示せない。
この段階では頑健な IC/bigram の方が信号が強い。プラグが増えて文が復元されるにつれ、
より高次の n-gram に切り替えると識別力が最大化される。

**性能実証**（同一候補・同一SA予算で、trigram単独SAと比較）:

| 暗号文 | プラグボード | 段階スコア(本手法) | trigram単独SA |
|---|---|---|---|
| 125字 | 8ペア | 完全解読 | 完全解読 |
| 126字 | 10ペア | 完全解読 | 完全解読 |
| 126字 | 9ペア | **完全解読** | 失敗（別候補を誤選択）|

中程度長×多プラグの難条件で段階スコアが優位。50〜65字の極短文は情報理論的限界に
近く、本手法でも余分なプラグを過学習することがある（どのアルゴリズムでも同じ壁）。

---

## プラグボード既知版 (`decrypt_known_plugboard.py`)

プラグボード設定が事前に判明している場合に使うツール。
山登り法（Phase 2）が不要になるため、**通常版より大幅に高速・高精度**。
Phase 1 は Rust + Rayon で並列化されており（`enigma_decoder` がビルド済みの場合）、
50〜100字の短文なら **1件あたり約1秒**で解読できる。

```bash
python decrypt_known_plugboard.py             # 対話モード
python decrypt_known_plugboard.py --selftest  # 動作確認
python decrypt_known_plugboard.py --lang english <暗号文>
python decrypt_known_plugboard.py --accuracy <暗号文>   # リングを 676通り探索
```

ハードコードされたプラグボードは `decrypt_known_plugboard.py` 冒頭の
`KNOWN_PLUGBOARD` 定数を書き換えて変更する。

**通常版との比較**:

| | 通常版 (`decrypt.py`) | 既知版 (`decrypt_known_plugboard.py`) |
|---|---|---|
| Phase 1 | IC スコア（プラグボード無し）| n-gram スコア（既知 PB 適用、Rust並列）|
| Phase 2 | 山登りで PB を推定（重い）| **不要** |
| 短文精度 | プラグボード数に依存 | **大幅向上**（単語リスト照合併用）|

**性能実測**（50〜100字・非ゼロリング設定・5ペアPB・最高精度モード）:

| Phase 1 実装 | 5ケース総時間 | 精度 |
|---|---|---|
| 純Python | 412秒 | 5/5 完全一致 |
| **Rust + Rayon** | **5.1秒（約80倍高速）** | 5/5 完全一致 |

Rust 拡張が未ビルドの環境では自動的に純Python版にフォールバックする
（結果は同一、速度のみ異なる）。ビルドは以下:

```bash
cd enigma_decoder && maturin build --release
pip install target/wheels/enigma_decoder-*.whl
```

---

## 基本の使い方

VSCodeで `decrypt.py` を開いて▷ボタンを押すか、ターミナルで：

```bash
python decrypt.py             # 対話モード（暗号文・言語・モードを順に尋ねる）
python decrypt.py --selftest  # 既知平文で動作確認
```

対話モードでは以下を順に聞かれます：

1. **暗号文** をコピペして Enter
2. **平文の言語** を 1-3 で選択（auto / english / romaji）
3. **探索モード** を 1-3 で選択（通常 / 精度 / 徹底）

## 3つの探索モード

### モード1: 通常 (normal)

**約1〜3分**。0〜2ペアのプラグボードに最適化。

- Phase 1: ローター・位置を全探索（60×17576 通り）、上位 200 候補を保持
- Phase 2: 高速プラグボード山登り（追加のみ、開始点1つ）
- Refine: 右ローターのリング設定 26 通り

| 文字数 | プラグボード | 期待精度 |
|---|---|---|
| 200+ | 0-2ペア | ほぼ100% |
| 100-200 | 0-2ペア | 90%以上 |
| 100-200 | 3ペア | 60-80%（要精度モード） |
| 100以下 | 3+ペア | 不安定（要精度/徹底モード） |

### モード2: 精度 (accuracy)

**約10〜30分**。短文・多めプラグボードに対応。

- Phase 1: 上位 2000 候補（通常の10倍）、サンプル長 200字
- Phase 2-1: 2000候補全部に高速山登り（ふるい分け）
- Phase 2-2: 上位 200 候補に **完全山登り**＋ランダム再開2回
  - 既存ペアの解除・再配置を含む Gillogly 本来の山登り
  - 局所最適に陥っても複数開始点で回避
- Refine: 中・右ローターのリング設定 26×26 通り

実証データ:

| ケース | 通常 | 精度 |
|---|---|---|
| 90字英文 + 3ペア | 8.9% | **100%** |
| 192字英文 + 2ペア | 100% | 100% |

### モード3: 徹底 (thorough)

**数時間〜（時間無制限）**。極端に困難な条件向け。

- Phase 1: 上位 20000 候補、サンプル長 300字
- Phase 2-1/2: 同方式だが規模が大きく、再開4回
- 4+ペアプラグボード や 30字台の極短文 に最後の望みを賭けるとき用

PCの性能や暗号文長によっては丸一日かかることもあります。バックグラウンドで動かすのが現実的。

## コマンドラインオプション

```bash
# 言語を最初から指定
python decrypt.py --lang english <暗号文>
python decrypt.py --lang romaji  <暗号文>

# モードを最初から指定
python decrypt.py --accuracy <暗号文>
python decrypt.py --thorough <暗号文>

# 暗号文を引数で渡す（対話モードをスキップ）
python decrypt.py ZWIYNSYAYCIALZVKMEVRKJW...
python decrypt.py --lang romaji --accuracy ZWIYNSYAYCIAL...
```

## アルゴリズム詳細

ベースは **Gillogly 法（1995）** の二段階攻撃。

**Phase 1: ローター・初期位置の全探索**
- ローター I-V から3つ選ぶ全 60 通り × 初期位置 26³ ≈ 100 万通り
- 各設定でプラグボード無しで先頭 N 文字を復号
- 英語・ローマ字の **trigram + quadgram** 対数確率（指定言語のみ、または両言語の最大値）で順位付け

**Phase 2: プラグボードの山登り**
- 通常: 各反復で「未使用文字ペアを追加」のみ試行（高速だが局所最適）
- 精度/徹底: 各反復で「ペアを設定（既存ペア再配置含む）」「ペアを解除」全操作を試行（Gillogly 本来形）
- 精度/徹底: 空プラグボード + ランダム初期プラグボード（3-5ペア）から複数回再開

**Refine: リング設定**
- ノッチ越えタイミングがリング設定で変わるため、右ローター（および精度モードでは中ローター）のリングを再探索

## 結果の読み方

復号後、上位5候補がスコア順に表示されます。

**スコア差で信頼度を判定**:
- 第1候補と第2候補の差 > 0.5 → 「第1候補がほぼ確実に正解」
- 差 > 0.2 → 「第1候補が有力」
- それ以下 → 「複数候補を目視で確認」

スコア差が小さいときは、上位5候補を見比べて文意の通る方を選んでください。

## 解読の現実的な限界

短文＋クリブ無し＋プラグボードあり、はエニグマ攻撃の中で**最も難しい**シナリオです。情報理論的な下限があり、どんなアルゴリズムでも以下が壁になります：

**暗号文長**:
- 200文字以上 → 安定して解読可能（通常モードで足りる）
- 100〜200文字 → 通常モードで多くの場合可、3ペア以上なら精度モード
- 50〜100文字 → 精度モード推奨、3+ペアなら徹底モード
- 50文字未満 → 統計的限界に近い。徹底モードでも複数候補から目視必須

**プラグボード接続数**:
- 0〜2 ペア → ほぼ確実に解読
- 3 ペア → 短文では精度モード必須
- 4〜5 ペア → 徹底モードで挑戦、結果は文の長さ次第
- 6+ ペア → 短文では原理的に困難（軍用級設定）

カジュアルな暗号通信（友人間など）では通常 0〜3 ペアなので、精度モードまでで足ります。

## カスタマイズ

**コーパスを増強して精度向上**:
- `corpora.py` の `ENGLISH_CORPUS` / `ROMAJI_CORPUS` に実際の友人の文体に近いテキストを追加
- 多ければ多いほど n-gram 統計が精度向上

**M4（4ローター海軍式）への拡張**:
- `enigma.py` に第4ローター（β/γ）と薄型リフレクター（B-thin/C-thin）を追加
- 探索空間が約 26 倍になるので並列化推奨

**並列化で高速化**:
- Phase 1, Phase 2-1 ともに候補ごとに独立なので `multiprocessing.Pool` で簡単に並列化可能
- コア数倍のスピードアップが見込める

## 既知の限界

- リフレクター B のみ対応（C は `enigma.py` に定義済みだが攻撃ループは B 固定）
- 山登りは局所最適に陥る可能性あり（精度・徹底モードで複数開始点を試すことで部分的に緩和）
- 純Python実装。NumPy / Cython で書き直すと10〜100倍高速化可能
